using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MapInfo : MonoBehaviour
{
    //holds map for some reason? probally cause this class can easily be in diffrent scenes compared to MapGen
    public MapHolder map;
    public Map currentMap;

    public bool select;
}
